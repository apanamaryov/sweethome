{
  "name": "inverter-monitor-root",
  "private": true,
  "workspaces": ["server"],
  "scripts": {
    "build": "npm run build -w server",
    "check": "npm run check -w server"
  },
  "engines": {
    "node": ">=20"
  }
}
